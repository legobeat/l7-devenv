#!/bin/bash
export RUNNER_IMAGE=${PNPM_RUNNER_IMAGE:-${NODE_RUNNER_IMAGE:-${RUNNER_IMAGE}}}
sudo --preserve-env=CONTAINER_HOST,container,TERM \
  podman run --rm -it "${RUNNER_IMAGE}" \
  -- pnpm ${@}
